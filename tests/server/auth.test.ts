import { describe, it } from "vitest";

describe("auth API", () => {
  it.todo("/api/auth/me returns null for unauthenticated");
  it.todo("/api/auth/register creates user");
  it.todo("/api/auth/login returns session");
});
